import { View, Text, Button, SafeAreaView } from 'react-native'

export default function DashBoard({ navigation }) {
    return (
        <SafeAreaView>
            <Text>DashBoard</Text>
        </SafeAreaView>
    )
}